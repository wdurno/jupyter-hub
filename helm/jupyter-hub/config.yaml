auth:
  type: azuread
  azuread:
    clientId: "CLI ARG"
    clientSecret: "CLI ARG"
    tenantId: "CLI ARG"
    callbackUrl: "CLI ARG"

proxy:
  secretToken: "CLI ARG"
  https:
    hosts:
     - "jupyterhub12345.eastus.cloudapp.azure.com"
    type: secret
    secret:
      name: jh-tls
